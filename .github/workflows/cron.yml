# name: Streak Notification Cron

# on:
#   schedule:
#     - cron: '*/5 * * * *'  # Runs every 5 minutes
#   workflow_dispatch:        # Allows manual triggering

# jobs:
#   run-streak-cron:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Ping streak script
#         run: |
#           curl -s https://your-domain.com/send-streak.php
