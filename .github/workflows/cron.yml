name: 🔔 Cron Streak Notifier

on:
  schedule:
    - cron: '*/5 * * * *' # Runs every 5 minutes (UTC)
  workflow_dispatch:      # Allows manual run via GitHub UI

jobs:
  run-streak-notification:
    runs-on: ubuntu-latest

    steps:
      - name: Call Render PHP Script for Notifications
        run: |
          curl -X GET "https://firestorm-streak.onrender.com/app/health-check?action=admin-send-notification"
